<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Auth" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/purple_700</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/light_red</item>
        <item name="windowSplashScreenAnimationDuration">5000</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_security</item>
<!--        Set the theme of the Activity that directly follows the splash screen -->
<!--        Required -->
        <item name="postSplashScreenTheme">@style/Theme.Auth</item>
    </style>
</resources>